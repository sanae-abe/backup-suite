# backup-suite

[![Crates.io](https://img.shields.io/crates/v/backup-suite.svg)](https://crates.io/crates/backup-suite)
[![Documentation](https://docs.rs/backup-suite/badge.svg)](https://docs.rs/backup-suite)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Rust](https://img.shields.io/badge/rust-1.82%2B-blue.svg)](https://www.rust-lang.org)
[![CI](https://github.com/sanae-abe/backup-suite/workflows/CI/badge.svg)](https://github.com/sanae-abe/backup-suite/actions)

[日本語](README.md) | [English](README.en.md) | [简体中文](README.zh-CN.md) | [繁體中文](README.zh-TW.md)

> **高速、安全、智能的本地备份工具**

## 目录

- [核心功能](#核心功能)
- [截图展示](#截图展示)
- [安装方法](#安装方法)
- [快速入门](#快速入门)
- [基本使用](#基本使用)
- [AI 功能（智能备份）](#-smart-功能智能备份)
- [配置文件](#配置文件)
- [调度功能](#调度功能)
- [命令参考](#命令参考)
- [更新与卸载](#更新与卸载)
- [安全与质量](#安全与质量)
- [技术栈](#技术栈)
- [支持平台](#支持平台)
- [开源协议](#开源协议)

## 核心功能

### 🎯 优先级备份管理
- **重要工作文件**每天自动备份
- **照片和个人文件**每周备份
- **归档文件**每月备份

### 🤖 AI 驱动的智能管理
- **自动优化**：通过目录分析自动生成最佳备份配置
- **文件重要性分析**：自动按重要程度分类目录中的文件（约 8 秒/10,000 个文件）
- **排除模式推荐**：自动检测并建议排除不必要的文件（缓存、构建产物）
- **异常检测**：通过统计分析自动检测备份大小异常（< 1ms）
- **完全离线**：所有 AI 功能均在本地运行，完全保护隐私

### 🔐 军事级加密保护
- **AES-256-GCM 加密**几乎不可能破解
- **Argon2 密钥派生**从密码生成安全的加密密钥
- **电脑被盗**时数据也完全安全
- **云端存储**时第三方无法访问
- **强密码自动生成**确保安全性

### 📦 高速压缩节省存储空间
- **Zstd 压缩**实现高速和高压缩率
- **Gzip 压缩**注重兼容性
- **无压缩**选项也可选
- **磁盘空间最多减少 70%**（文本文件的典型情况）

### ⚡ 增量备份实现超高速
- **仅备份更改的文件**大幅缩短时间
- **SHA-256 哈希**准确检测更改
- **备份时间减少 90%**（第二次起，更改率 10% 的情况下）
- **存储容量减少 85%**（仅保存差异，典型情况）
- **自动切换到完整备份**（首次运行时）

### ⏰ 完全自动化的计划任务
- **配置后无需手动操作**自动执行
- **按重要性调整频率**（每日、每周、每月）
- **完全防止忘记备份**
- **macOS launchd/Linux systemd 集成**实现可靠的自动执行

### 📊 清晰的管理与维护
- **备份了多少**通过统计查看
- **何时执行**通过历史记录查看
- **旧备份**自动删除节省磁盘
- **数据损坏时**轻松恢复

### 🌍 多语言支持
- **完全支持 4 种语言**：日语、英语、简体中文（中国大陆）、繁体中文（台湾·香港）
- **自动语言检测**：从 `LANG` 环境变量自动判断（支持 `ja`、`en`、`zh-CN`、`zh-TW` 等）
- **所有消息已翻译**：CLI 输出、错误消息、帮助文本均以各语言显示

## 截图展示

### 帮助界面
<img src="docs/screenshots/help.webp" alt="backup-suite help" width="600">

*以日语显示命令列表和选项*

### 备份目标列表
<img src="docs/screenshots/list.webp" alt="backup-suite list" width="600">

*以表格形式显示已注册的备份目标*

### 备份执行
<img src="docs/screenshots/run.webp" alt="backup-suite run" width="600">

*实际备份执行界面*

### 备份执行（试运行）
<img src="docs/screenshots/dry-run.webp" alt="backup-suite dry-run" width="600">

*不实际复制文件，仅确认执行内容*

### 备份历史
<img src="docs/screenshots/history.webp" alt="backup-suite history" width="600">

*查看过去的备份执行历史*

## 安装方法

### 通过 Homebrew 安装（macOS）

```bash
brew tap sanae-abe/backup-suite
brew install backup-suite
```

### 通过 Cargo 安装

```bash
# 启用 AI 功能安装（推荐）
cargo install backup-suite --features smart

# 不启用 AI 功能安装（轻量版）
cargo install backup-suite
```

### 从源码构建

```bash
# 1. 克隆仓库
git clone git@github.com:sanae-abe/backup-suite.git
cd backup-suite

# 2. 安装 Rust（如果尚未安装）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source ~/.cargo/env

# 3. 构建并安装（带 AI 功能）
cargo build --release --features smart
cargo install --path . --features smart

# 4. 验证运行
backup-suite --version
```

### 🌍 Shell 补全（多语言支持）

Shell 补全支持 **4 种语言**：英语、日语、简体中文、繁体中文。

#### 快速设置（Zsh）

```bash
# 1. 创建补全目录
mkdir -p ~/.zfunc

# 2. 添加到 ~/.zshrc
echo 'fpath=(~/.zfunc $fpath)' >> ~/.zshrc
echo 'autoload -Uz compinit && compinit' >> ~/.zshrc

# 3. 生成补全（从 $LANG 自动检测语言）
backup-suite completion zsh > ~/.zfunc/_backup-suite

# 4. 重启 shell
exec zsh
```

**手动选择语言**：

```bash
# 日语
./scripts/generate-completion.sh ja

# 简体中文
./scripts/generate-completion.sh zh-CN

# 繁体中文
./scripts/generate-completion.sh zh-TW

# 英语
./scripts/generate-completion.sh en
```

**故障排除**：

如果补全不工作，请参阅详细指南 [docs/shell-completion.md](docs/shell-completion.md)。常见解决方案：

- **完全没有补全**：重启 shell（`exec zsh`），检查文件是否存在（`ls -la ~/.zfunc/_backup-suite`）
- **显示错误的语言**：检查 `echo $LANG`，或使用 `./scripts/generate-completion.sh zh-CN` 手动指定
- **compinit 警告**：修复权限（`chmod go-w ~/.zfunc`）

有关 Bash/Fish 安装和详细故障排除，请参阅 [docs/shell-completion.md](docs/shell-completion.md)。

## 快速入门

### 1. 基本设置
```bash
# 查看当前配置
backup-suite status

# 配置文件位置
# ~/.config/backup-suite/config.toml
```

**注意**：语言由环境变量 `LANG` 自动检测。支持的语言：日语、英语、简体中文、繁体中文。在日语环境中会自动以日语显示。

### 2. 设置备份保存路径

```bash
# 设置 Google Drive 保存路径
backup-suite config set-destination "/Users/你的用户名/Library/CloudStorage/GoogleDrive-your@email.com/我的云端硬盘/backup-storage"

# ⚠️ 重要：备份到云存储时必须启用加密
# 将备份保存到 Google Drive 等云存储时，
# 为防止第三方未授权访问，请务必使用 --encrypt 选项

# 查看当前配置
backup-suite config get-destination
```

### 3. 验证配置

要验证配置，请使用 [1. 基本设置](#1-基本设置) 中的 `backup-suite status` 命令。

## 基本使用

1. **添加文件**
```bash
backup-suite add ~/Documents/project --priority high --category development
backup-suite add ~/Photos --priority medium --category personal
```

2. **查看目标列表**
```bash
backup-suite list
backup-suite list --priority high  # 仅高优先级
```

3. **执行备份**
```bash
backup-suite run                   # 执行所有目标
backup-suite run --priority high   # 仅高优先级
backup-suite run --category work   # 仅特定类别
backup-suite run --dry-run         # 试运行（仅确认）

# 增量备份
backup-suite run --incremental     # 仅备份更改部分（第二次起推荐）

# 压缩选项
backup-suite run --compress zstd   # Zstd 压缩（高速·高压缩率·推荐）
backup-suite run --compress gzip   # Gzip 压缩（注重兼容性）
backup-suite run --compress none   # 无压缩

# 加密备份（推荐：交互式密码提示）
backup-suite run --encrypt
# → 通过提示安全输入密码（不会保存在 Shell 历史记录中）

# 或使用环境变量（可选）
export BACKUP_SUITE_PASSWORD="your-secure-password"
backup-suite run --encrypt

# 压缩 + 加密组合
backup-suite run --compress zstd --encrypt
# → 通过提示交互式输入密码
```

4. **自动化设置**
```bash
# 按优先级设置计划
backup-suite schedule setup --high daily --medium weekly --low monthly
backup-suite schedule enable
```

## 🤖 AI 功能（智能备份）

通过统计异常检测和文件重要性分析优化备份。

### 安装

要使用 AI 功能，需要在构建时使用 `--features smart` 标志。

```bash
# 启用 AI 功能构建
cargo build --release --features smart
cargo install --path . --features smart

# 或通过 Cargo 安装
cargo install backup-suite --features smart
```

### 主要功能

#### 1. AI 自动配置

分析目录并自动生成最佳备份配置。

```bash
# 自动分析和配置（分别评估子目录）
backup-suite smart auto-configure ~/data

# 以交互方式确认并配置（确认子目录和排除模式）
backup-suite smart auto-configure ~/data --interactive

# 试运行（不应用配置，仅确认）
backup-suite smart auto-configure ~/data --dry-run

# 指定子目录探索深度（默认：1）
backup-suite smart auto-configure ~/data --max-depth 2

# 指定要处理的子目录最大数（默认：100）
backup-suite smart auto-configure ~/data --max-subdirs 50

# 大量子目录时增加处理数上限
backup-suite smart auto-configure ~/data --max-subdirs 200
```

**功能**：
- **分别评估每个子目录的重要性**（为每个目录设置最佳优先级）
- **自动检测并应用排除模式**（自动排除 `node_modules/`、`target/`、`.cache/` 等）
- **自动判断项目类型**（Rust、Node.js、Python 等）
- **仅应用置信度 80% 以上的模式**（防止误检）
- **通过处理数限制进行性能优化**（`--max-subdirs` 应对大量子目录，默认：100）

**输出示例**：
```
🤖 AI 自动配置
分析中："/Users/user/projects"
  📁 发现 3 个子目录：3
    评估中："/Users/user/projects/web-app"
      推荐优先级：High（分数：95）
      📋 排除模式建议：3
        - node_modules（99.0%，预计减少 2.34 GB）
        - .cache（95.0%，预计减少 0.45 GB）
        - .*\.tmp$（99.0%，预计减少 0.00 GB）
      📝 排除模式：node_modules、.cache、.*\.tmp$
      ✅ 已添加到配置
    评估中："/Users/user/projects/rust-cli"
      推荐优先级：High（分数：95）
      📋 排除模式建议：2
        - target（99.0%，预计减少 1.87 GB）
        - .cache（95.0%，预计减少 0.12 GB）
      📝 排除模式：target、.cache
      ✅ 已添加到配置
    评估中："/Users/user/projects/archive"
      推荐优先级：Low（分数：30）
      ✅ 已添加到配置

自动配置已完成
  添加的项目：3
  总减少量：4.78 GB（备份时间约缩短 35%）
```

**子目录数达到限制时的示例**：
```
🤖 AI 自动配置
分析中："/Users/user/large-project"
  📁 发现 100 个子目录：100
  ⚠️  已达到限制，部分子目录未处理：100（可使用 --max-subdirs 更改）
```

#### 2. 文件重要性分析

按重要程度对目录中的文件进行分类，优化备份策略。

```bash
# 分析目录重要性
backup-suite smart analyze ~/documents

# 显示详细的重要性分数
backup-suite smart analyze ~/documents --detailed

# 仅分析特定文件类型
backup-suite smart analyze ~/projects --filter "*.rs,*.toml"
```

**评估标准**：
- **高重要性（80-100 分）**：源代码、文档、配置文件
- **中重要性（40-79 分）**：图像、数据文件
- **低重要性（0-39 分）**：日志、临时文件

**输出示例**：
```
🤖 AI 文件重要性分析：~/Documents

┌─────────────────────────┬──────────────┬──────────┬─────────────────────┐
│ 文件/目录                │ 重要性分数    │ 建议优先级 │ 理由                 │
├─────────────────────────┼──────────────┼──────────┼─────────────────────┤
│ src/                    │ ████████ 95  │ 高        │ 源代码（频繁更新）    │
│ reports/                │ ████████ 90  │ 高        │ 文档（重要）          │
│ photos/                 │ ████░░░░ 60  │ 中        │ 图像文件              │
│ .cache/                 │ █░░░░░░░ 10  │ 建议排除  │ 缓存目录              │
└─────────────────────────┴──────────────┴──────────┴─────────────────────┘
```

**性能**：约 8 秒（10,000 个文件）

#### 3. 排除模式推荐

自动检测不必要的文件，推荐排除模式。

```bash
# 显示排除模式推荐
backup-suite smart suggest-exclude ~/projects

# 自动将推荐模式应用到配置文件
backup-suite smart suggest-exclude ~/projects --apply

# 指定最小文件大小（默认：100MB）
backup-suite smart suggest-exclude ~/projects --min-size 50MB
```

**检测目标**：
- 构建产物（`target/`、`dist/`、`build/`）
- 依赖缓存（`node_modules/`、`.cargo/`）
- 临时文件（`*.tmp`、`*.cache`）
- 大容量媒体文件（超过阈值大小）

**输出示例**：
```
🤖 AI 排除模式推荐：~/projects

┌──────────────────┬──────────┬──────────┬─────────────────────┐
│ 模式              │ 减少量    │ 置信度    │ 理由                 │
├──────────────────┼──────────┼──────────┼─────────────────────┤
│ node_modules/    │ 2.34 GB  │ 99%      │ npm 依赖（可重新生成）│
│ target/          │ 1.87 GB  │ 99%      │ Rust 构建产物        │
│ .cache/          │ 0.45 GB  │ 95%      │ 缓存目录              │
└──────────────────┴──────────┴──────────┴─────────────────────┘

💡 总减少量：4.66 GB（备份时间约缩短 30%）
```

#### 4. 异常检测

从历史记录中检测统计上异常的备份。

```bash
# 检测过去 7 天的异常
backup-suite smart detect --days 7

# 更详细的分析（也显示统计信息）
backup-suite smart detect --days 14 --detailed
```

**检测内容**：
- 备份大小的急增/急减（Z-score 统计分析）
- 磁盘容量耗尽预测（线性回归）
- 失败模式分析（按类别和时间）

**输出示例**：
```
🤖 AI 异常检测报告（过去 7 天）

┌────┬──────────────────┬──────────┬──────────┬─────────────────────┐
│ No │ 检测时间          │ 异常类型  │ 置信度    │ 说明                 │
├────┼──────────────────┼──────────┼──────────┼─────────────────────┤
│ 1  │ 2025-11-09 03:15 │ 大小激增  │ 95.3%    │ 文件大小为正常的3倍   │
└────┴──────────────────┴──────────┴──────────┴─────────────────────┘

📊 摘要：检测到 1 个异常
💡 推荐操作：将 ~/Downloads 的临时文件添加到排除设置
```

**性能**：< 1ms（100 条历史记录）

### 禁用 AI 功能

如果不需要 AI 功能，请使用标准构建。

```bash
# 标准构建（无 AI 功能）
cargo build --release
cargo install --path .
```

### 安全与隐私

所有 AI 功能**完全离线**运行：

- ✅ 外部 API 调用：无
- ✅ 云服务：不需要
- ✅ 敏感信息传输：零
- ✅ 数据收集：无

详情请参阅 [AI 功能文档](docs/smart/features.md)。

## 配置文件

### ~/.config/backup-suite/config.toml 示例
```toml
[general]
log_level = "info"
log_file = "~/.local/share/backup-suite/logs/backup.log"

[storage]
type = "local"
path = "/Users/john/Library/CloudStorage/GoogleDrive-john@example.com/我的云端硬盘/backup-storage"  # 使用云存储时 encryption = true 是必需的
compression = "zstd"  # 压缩类型："zstd"、"gzip"、"none"
compression_level = 3  # 压缩级别：1-22（Zstd）、1-9（Gzip）
encryption = true
encryption_key_file = "~/.config/backup-suite/keys/backup.key"  # 重要：使用 chmod 600 保护

[schedule]
enabled = true
daily_time = "02:00"
weekly_day = "sunday"
monthly_day = 1

[targets]
[[targets.directories]]
name = "documents"
path = "~/Documents"
exclude = ["*.tmp", "*.cache", ".DS_Store"]

[[targets.directories]]
name = "projects"
path = "~/Projects"
exclude = ["node_modules/", "target/", ".git/", "*.log"]
```

## 计划任务功能

### 自动备份设置

```bash
# 设置计划频率
backup-suite schedule setup --high daily --medium weekly --low monthly

# 启用计划
backup-suite schedule enable

# 查看状态
backup-suite schedule status
```

### 各平台的运作方式

#### macOS (launchd)
- 配置文件：`~/Library/LaunchAgents/com.backup-suite.{priority}.plist`
- 日志：`/tmp/backup-suite-{priority}.log`
- 确认：`launchctl list | grep backup-suite`

#### Linux (systemd)
- 配置文件：`~/.config/systemd/user/backup-suite-{priority}.{service,timer}`
- 日志：`journalctl --user -u backup-suite-{priority}.service`
- 确认：`systemctl --user list-timers backup-suite-*`

详情请参阅[计划任务指南](docs/SCHEDULER.md)。

## 命令参考

| 命令           | 说明                       | 示例                                            |
| -------------- | -------------------------- | ----------------------------------------------- |
| **add**        | 添加备份目标               | `backup-suite add ~/docs --priority high`       |
| **list, ls**   | 显示目标列表               | `backup-suite list --priority medium`           |
| **remove**     | 删除目标                   | `backup-suite remove ~/old-files`               |
| **clear, rm**  | 批量删除                   | `backup-suite clear --priority low`             |
| **run**        | 执行备份                   | `backup-suite run --encrypt`                    |
| **restore**    | 恢复备份                   | `backup-suite restore --from backup-20251104`   |
| **cleanup**    | 删除旧备份                 | `backup-suite cleanup --days 30`                |
| **status**     | 显示当前状态               | `backup-suite status`                           |
| **history**    | 显示执行历史               | `backup-suite history --days 7`                 |
| **schedule**   | 计划任务管理               | `backup-suite schedule enable`                  |
| **config**     | 配置管理                   | `backup-suite config set-destination ~/backups` |
| **open**       | 打开备份目录               | `backup-suite open`                             |
| **completion** | 生成 shell 补全            | `backup-suite completion zsh`                   |
| **smart**         | AI 功能（需要 `--features smart`）| `backup-suite smart detect --days 7`            |

## 更新与卸载

### 更新

```bash
# Homebrew
brew upgrade backup-suite

# Cargo
cargo install backup-suite --force --features smart

# 从源码
cd backup-suite
git pull origin main
cargo install --path . --force --features smart
```

### 卸载

```bash
# 1. 删除二进制文件
rm ~/.local/bin/backup-suite

# 2. 删除配置文件（可选）
rm -rf ~/.config/backup-suite/

# 3. 删除日志文件（可选）
rm -rf ~/.local/share/backup-suite/
```

## 安全与质量

### **企业级安全**
- **加密**：AES-256-GCM（认证加密）
- **密钥派生**：Argon2id（内存成本 19MB，迭代 2 次）
- **路径遍历防护**：Unicode 规范化（NFKC）、Null byte 检测、符号链接攻击防护
- **敏感数据清除**：使用 Zeroize（内存转储攻击防护）
- **仅本地**：不依赖云端，安全可靠
- **权限管理**：配置文件的适当权限设置

### **类型安全与内存安全**
- Rust 强大的类型系统最大程度减少运行时错误
- 内存安全保证（防止缓冲区溢出、内存泄漏）
- 编译时错误检测
- 敏感数据的安全清除（ZeroizeOnDrop）

### **防御目标攻击**
- ✅ **路径遍历攻击**：Unicode 规范化（NFKC）、Null byte 检测、符号链接验证
- ✅ **暴力破解攻击**：Argon2id 高成本密钥派生（内存 19MB，迭代 2 次）
- ✅ **内存转储攻击**：通过 Zeroize 立即清除敏感数据
- ✅ **中间人攻击**：AES-256-GCM 认证加密（检测篡改）
- ✅ **时序攻击**：通过恒定时间比较验证密钥
- ✅ **注入攻击**：通过 Rust 类型系统实现安全的路径操作

## 技术栈

- **语言**：Rust（最新稳定版）
- **CLI**：clap 4.x（命令行解析·补全生成）
- **压缩**：Zstd（高速·高压缩率）、Gzip（兼容性）
- **加密**：AES-256-GCM、Argon2
- **配置**：TOML（人类可读的配置格式）
- **计划任务**：macOS launchctl、Linux systemd
- **Smart/统计分析**：statrs（统计计算）、rayon（并行处理）

## 支持平台

| 操作系统  | 架构           | 支持状态   |
| --------- | -------------- | ---------- |
| 🐧 Linux  | x86_64         | ✅ 完全支持 |
| 🐧 Linux  | aarch64        | ✅ 完全支持 |
| 🍎 macOS  | x86_64         | ✅ 完全支持 |
| 🍎 macOS  | Apple Silicon  | ✅ 完全支持 |

## 开源协议

本项目采用 [MIT 协议](LICENSE) 开源。

---

## 贡献

欢迎提交 Bug 报告、功能请求和 Pull Request。
请通过 GitHub Issues 或 PR 随时联系我们。
