# backup-suite v2.1.0 動作確認チェックリスト

このドキュメントは、backup-suite v2.1.0の主要機能が正しく動作するか確認するためのチェックリストです。

**所要時間**: 約20-30分
**対象バージョン**: v2.1.0 (2025-11-07リリース)

---

## 📋 事前準備

### 1. ビルド確認

```bash
cd /path/to/backup-suite
cargo build --release
```

**期待結果**: エラーなくビルド完了

**チェックポイント**:
- [x] `target/release/backup-suite` が生成される
- [x] コンパイル警告が致命的でない（未使用変数などは許容）

ユーザー確認結果： ✅

---

### 2. テスト環境の準備 

```bash
# テスト用ディレクトリ作成
mkdir -p ~/backup-test/files
mkdir -p ~/backup-suite/backups

# テスト用ファイル作成
echo "test content 1" > ~/backup-test/files/file1.txt
echo "test content 2" > ~/backup-test/files/file2.txt
echo "test content 3" > ~/backup-test/files/file3.txt
```

ユーザー確認結果： ✅

---

## 基本機能の確認（5項目）

### 3. ヘルプ表示

```bash
./target/release/backup-suite --help
```

**期待結果**: ヘルプメッセージが表示される

**チェックポイント**:
- [x] 全コマンド（add, run, restore, etc.）が表示される 
- [x] バージョン情報が含まれる

ユーザー確認結果：❌ オプションについての説明が不十分

---

### 4. バージョン確認 

```bash
./target/release/backup-suite --version
```

**期待結果**: `backup-suite 1.0.0` が表示される

ユーザー確認結果： ✅

---

## 🔐 パスワードポリシー機能（3項目）

### 5. パスワード生成機能

```bash
./target/release/backup-suite run --encrypt --generate-password --dry-run
```

**期待結果**: 20文字のランダムパスワードが生成される

**チェックポイント**:
- [x] 大文字・小文字・数字・記号を含む20文字のパスワードが表示される
- [x] パスワード強度が「強い」と表示される
- [x] dry-runなので実際のバックアップは実行されない

---

### 6. 弱いパスワードの警告

```bash
./target/release/backup-suite run --encrypt --password "123" --dry-run
```

**期待結果**: パスワード強度警告が表示される

**チェックポイント**:
- [x] 「パスワード強度: 弱い」と表示される
- [x] 警告メッセージが表示される
- [x] 実行は継続される（エラーではなく警告）

ユーザー確認結果： ✅

---

### 7. 強いパスワードの確認

```bash
./target/release/backup-suite run --encrypt --password "MyS3cur3!B@ckup#2024" --dry-run
```

**期待結果**: パスワード強度が「強い」と表示される

**チェックポイント**:
- [x] 「パスワード強度: 強い」と表示される
- [x] 警告が表示されない

ユーザー確認結果： ✅

---

## 📦 バックアップ機能（5項目）

### 8. 通常バックアップ（暗号化なし）

```bash
# バックアップ対象を追加
./target/release/backup-suite add ~/backup-test/files --priority high --category test

# バックアップ実行
./target/release/backup-suite run
```

**期待結果**: バックアップが成功する

**チェックポイント**:
- [x] 進捗バーが表示される
- [x] 「✓ バックアップ完了」と表示される
- [x] `~/backup-suite/backups/backup_YYYYMMDD_HHMMSS/test/` にファイルがコピーされる
- [x] 3ファイルがバックアップされる

ユーザー確認結果： ✅

---

### 9. 整合性検証機能

前の手順で作成されたバックアップディレクトリを確認：

```bash
# 最新のバックアップディレクトリを確認
ls -la ~/backup-suite/backups/backup_*/
```

**期待結果**: `.integrity` ファイルが存在する

**チェックポイント**:
- [x] `.integrity` ファイルが各バックアップディレクトリに存在する
- [x] JSON形式でファイルハッシュ情報が含まれる

```bash
# .integrityファイルの内容確認
cat ~/backup-suite/backups/backup_*/\.integrity
```

**期待される内容**:
```json
{
  "version": "1.0",
  "timestamp": "2025-11-07T...",
  "backup_type": "Full",
  "file_hashes": {
    "test/file1.txt": "...",
    "test/file2.txt": "...",
    "test/file3.txt": "..."
  }
}
```

ユーザー確認結果： ✅

---

### 10. 暗号化バックアップ

```bash
./target/release/backup-suite run --encrypt --password "MyS3cur3!B@ckup#2024"
```

**期待結果**: 暗号化されたバックアップが作成される

**チェックポイント**:
- [x] バックアップが成功する
- [x] バックアップファイルが暗号化されている（`cat`で内容が読めない）

```bash
# 暗号化されたファイルを確認（バイナリデータが表示されるはず）
xxd ~/backup-suite/backups/backup_*/test/file1.txt | head -5
```

ユーザー確認結果： ✅

---

### 11. 増分バックアップ（初回 - フルバックアップフォールバック）

```bash
./target/release/backup-suite run --incremental
```

**期待結果**: 初回なのでフルバックアップにフォールバックする

**チェックポイント**:
- [ ] 「ℹ️  前回のバックアップが見つかりません。フルバックアップを実行します。」と表示される 
- [x] 「📦 フルバックアップモード（全ファイル）」と表示される
- [x] 全ファイルがバックアップされる

---

### 12. 増分バックアップ（2回目 - 変更ファイルのみ）

```bash
# ファイルを変更
echo "modified content" > ~/backup-test/files/file1.txt

# 増分バックアップ実行
./target/release/backup-suite run --incremental
```

**期待結果**: 変更されたファイルのみバックアップされる

**チェックポイント**:
- [x] 「📦 増分バックアップモード（変更ファイルのみ）」と表示される
- [x] 「変更ファイル数: 1/3」などと表示される
- [x] file1.txtのみがバックアップされる

ユーザー確認結果： ✅

---

## 🔄 復元機能（1項目）

### 13. 増分復元（チェーン自動解決）

```bash
# 最新のバックアップから復元
./target/release/backup-suite restore --from backup_YYYYMMDD_HHMMSS --to ~/backup-test/restored
```

**期待結果**: 増分チェーンが自動解決されて復元される

**チェックポイント**:
- [x 増分バックアップチェーンが検出される（複数回増分バックアップした場合）
- [x] 「📦 増分バックアップチェーン検出: X 個のバックアップを順次復元」と表示される
- [x] 全ファイルが正しく復元される
- [x] 整合性検証が成功する（「✓ 整合性検証」と表示される）

```bash
# 復元されたファイルを確認
cat ~/backup-test/restored/backup_*/test/file1.txt
# 期待: "modified content" が表示される（最新の変更が反映）
```

ユーザー確認結果： ✅

---

### 14. 暗号化バックアップの復元

```bash
# 暗号化されたバックアップから復元
./target/release/backup-suite restore --from backup_YYYYMMDD_HHMMSS --to ~/backup-test/restored-encrypted --password "MyS3cur3!B@ckup#2024"
```

**期待結果**: 暗号化されたバックアップが復号化されて復元される

**チェックポイント**:
- [x] パスワード入力後、復号化が成功する
- [x] 復元されたファイルが読める（平文）

ユーザー確認結果： ✅

---

## 📊 監査ログ機能（2項目）

### 15. 監査ログの確認

```bash
# macOS
cat ~/Library/Application\ Support/backup-suite/audit.log

# Linux
cat ~/.config/backup-suite/audit.log
```

**期待結果**: JSON形式の監査ログが記録されている

**チェックポイント**:
- [x] 各バックアップ・復元操作がイベントとして記録されている
- [x] 必須フィールド（timestamp, event_type, user, target, metadata, hmac）が全て含まれる

**サンプルエントリ**:
```json
{
  "timestamp": "2025-11-07T09:00:00.000000Z",
  "event_type": "BackupStarted",
  "user": "username",
  "target": "priority=Some(High), category=Some(\"test\")",
  "metadata": null,
  "hmac": "abc123..."
}
```

ユーザー確認結果： ✅

---

### 16. HMAC改ざん検出機能

```bash
# 監査ログのテストを実行してHMAC機能を確認

```

**期待結果**: 13個の監査ログテストが全て合格する

**チェックポイント**:
- [x] `test_audit_event_creation_and_hmac` - HMAC計算と基本検証が正しい
- [x] `test_audit_log_hmac_with_different_keys` - 異なる鍵でのHMAC検証（鍵違い検出）
- [x] `test_audit_log_tampering_detection` - 改ざん検出が機能する（ファイル書き換え検出）
- [x] 全13テストが passed

ユーザー確認結果：✅（警告は無視可能、全テスト合格）

backup-suite on  feature/v2.1.0-security-and-incremental [!?] is 󰏗 v1.0.0 via 󱘗 v1.91.0 on   sanae-abe@m3.com
❯ LANG=en_US.UTF-8 cargo test --test audit_tests -- --nocapture
warning: comparison is useless due to type limits
   --> tests/audit_tests.rs:186:13
    |
186 |     assert!(result.deleted >= 0); // ドライランでなければ削除される
    |             ^^^^^^^^^^^^^^^^^^^
    |
    = note: `#[warn(unused_comparisons)]` on by default

warning: `backup-suite` (test "audit_tests") generated 1 warning
    Finished `test` profile [unoptimized + debuginfo] target(s) in 0.97s
     Running tests/audit_tests.rs (target/debug/deps/audit_tests-6832a5f165488571)

running 13 tests
test test_audit_log_permission_security ... ok
test test_audit_log_json_serialization ... ok
test test_audit_event_creation_and_hmac ... ok
test test_audit_log_hmac_with_different_keys ... ok
test test_current_user_detection ... ok
警告: 1行目のログエントリのHMAC検証に失敗しました
test test_audit_log_error_recovery ... ok
⠂
  バックアップ対象ファイルを収集中...
  1ファイルを検出                                                     📦 フルバックアップモード（全ファイル）
警告: 整合性メタデータの読み込みに失敗しました ("/var/folders/98/m4zdxqj97xb1gyyybg23wsm80000gr/T/.tmp0j17ms/backup"): 整合性メタデータが見つかりません: "/var/folders/98/m4zdxqj97xb1gyyybg23wsm80000gr/T/.tmp0j17ms/backup/.integrity"
[00:00:00] ████████████████████████████████████████ 1/1 ファイル (100%) ETA: 0s ✓ バックアップ完了                                          test test_audit_log_various_event_types ... ok
test test_restore_engine_with_audit_logging ... ok
🗑️  削除完了: "/var/folders/98/m4zdxqj97xb1gyyybg23wsm80000gr/T/.tmpue4881/backups/backup_20200101_000000"
test test_cleanup_engine_with_audit_logging ... ok
test test_backup_runner_with_audit_logging ... ok
test test_audit_log_rotation ... ok
test test_audit_log_time_based_filtering ... ok

test result: ok. 13 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.12s

---

## 📋 管理機能（2項目）

### 17. バックアップ一覧表示

```bash
./target/release/backup-suite list
```

**期待結果**: バックアップ対象がテーブル形式で表示される

**チェックポイント**:
- [x] テーブルヘッダー（No, 優先度, 種別, パス, カテゴリ, 除外パターン, 追加日）が表示される
- [x] 追加したバックアップ対象が表示されるc

ユーザー確認結果：✅

---

### 18. 優先度フィルタ

```bash
# 高優先度のみ表示
./target/release/backup-suite list --priority high
```

**期待結果**: 高優先度のバックアップ対象のみ表示される

ユーザー確認結果：✅

---

## ❌ エラーハンドリング（1項目）

### 19. 存在しないバックアップからの復元

```bash
./target/release/backup-suite restore --from nonexistent_backup_12345 --to ~/backup-test/error-test
```

**期待結果**: 適切なエラーメッセージが表示される

**チェックポイント**:
- [x] 「❌ バックアップなし」などのエラーメッセージが表示される
- [x] クラッシュせずに正常終了する

ユーザー確認結果： ✅

---

## 🧪 全テスト実行（1項目）

### 20. 全テストスイート

```bash
# 英語ロケールで実行（パスワードポリシーテストがロケール依存のため）
LANG=en_US.UTF-8 cargo test
```

**期待結果**: 全343テストが合格する

**チェックポイント**:
- [x] Unit tests: 135 passed
- [x] Audit tests: 13 passed
- [x] Incremental tests: 4 passed
- [x] Integration tests: 16 passed
- [x] Integrity tests: 5 passed
- [x] Nonce verification: 5 passed
- [x] Phase 2 integration: 9 passed
- [x] Property tests: 14 passed
- [x] Crypto property tests: 10 passed
- [x] Security property tests: 13 passed
- [x] Security tests: 23 passed
- [x] Doc tests: 96 passed
- [x] **合計**: 343 passed, 2 ignored

**警告の確認**:
- `audit_tests.rs:186` の比較警告は無視してOK（機能に影響なし）

ユーザー確認結果： ✅

---

## 📝 チェックリスト完了確認

以下の項目が全て完了していることを確認してください：

### 基本機能（5項目）
- [x] 1. ビルド確認
- [x] 2. テスト環境準備
- [x] 3. ヘルプ表示
- [x] 4. バージョン確認

### パスワードポリシー（3項目）
- [x] 5. パスワード生成機能
- [x] 6. 弱いパスワード警告
- [x] 7. 強いパスワード確認

### バックアップ機能（5項目）
- [x] 8. 通常バックアップ（暗号化なし）
- [x] 9. 整合性検証機能
- [x] 10. 暗号化バックアップ
- [x] 11. 増分バックアップ（初回フォールバック）
- [x] 12. 増分バックアップ（2回目・変更のみ）

### 復元機能（2項目）
- [x] 13. 増分復元（チェーン自動解決）
- [x] 14. 暗号化バックアップの復元

### 監査ログ（2項目）
- [x] 15. 監査ログの確認
- [x] 16. HMAC改ざん検出機能

### 管理機能（2項目）
- [x] 17. バックアップ一覧表示
- [x] 18. 優先度フィルタ

### エラーハンドリング（1項目）
- [x] 19. 存在しないバックアップからの復元

### テスト（1項目）
- [x] 20. 全テストスイート（343テスト合格）

**合計**: 20項目

---

## 🎯 次のステップ

全チェックリストが完了したら：

1. **本番環境での試用**: 実際のデータで小規模テスト
2. **スケジューリング設定**: launchd/systemdでの自動実行設定
3. **長期運用計画**: クリーンアップポリシーの決定
4. **ドキュメント確認**: [USAGE.md](./USAGE.md) で詳細機能を確認

---

## 📞 サポート

問題が発生した場合：

1. [GitHub Issues](https://github.com/yourusername/backup-suite/issues) で報告
2. [README.md](../../README.md) のトラブルシューティングを確認
3. 監査ログ（`~/Library/Application Support/backup-suite/audit.log`）を確認

---

**最終更新**: 2025-11-07
**対象バージョン**: backup-suite v2.1.0
**ドキュメント作成**: Claude Code + セキュリティ監査
