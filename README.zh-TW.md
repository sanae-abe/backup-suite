# backup-suite

[![Crates.io](https://img.shields.io/crates/v/backup-suite.svg)](https://crates.io/crates/backup-suite)
[![Documentation](https://docs.rs/backup-suite/badge.svg)](https://docs.rs/backup-suite)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Rust](https://img.shields.io/badge/rust-1.82%2B-blue.svg)](https://www.rust-lang.org)
[![CI](https://github.com/sanae-abe/backup-suite/workflows/CI/badge.svg)](https://github.com/sanae-abe/backup-suite/actions)

[日本語](README.ja.md) | [English](README.md) | [简体中文](README.zh-CN.md) | [繁體中文](README.zh-TW.md)

> **高速、安全、智慧的本機備份工具**

## 目錄

- [核心功能](#核心功能)
- [螢幕截圖](#螢幕截圖)
- [安裝方法](#安裝方法)
- [快速入門](#快速入門)
- [基本使用](#基本使用)
- [Smart 功能（智慧備份）](#-smart-功能智慧備份)
- [設定檔](#設定檔)
- [排程功能](#排程功能)
- [指令參考](#指令參考)
- [更新與移除](#更新與移除)
- [安全與品質](#安全與品質)
- [技術堆疊](#技術堆疊)
- [支援平台](#支援平台)
- [開源授權](#開源授權)

## 核心功能

### 🎯 優先順序備份管理
- **重要工作檔案**每天自動備份
- **照片和個人檔案**每週備份
- **封存檔案**每月備份

### 🤖 Smart 驅動的智慧管理
- **自動最佳化**：透過目錄分析自動產生最佳備份設定
- **檔案重要性分析**：自動按重要程度分類目錄中的檔案（約 8 秒/10,000 個檔案）
- **排除模式建議**：自動偵測並建議排除不必要的檔案（快取、建置產物）
- **異常偵測**：透過統計分析自動偵測備份大小異常（< 1ms）
- **完全離線**：所有 Smart 功能均在本機執行，完全保護隱私

### 🔐 軍事級加密保護
- **AES-256-GCM 加密**幾乎不可能破解
- **Argon2 金鑰衍生**從密碼產生安全的加密金鑰
- **電腦遭竊**時資料也完全安全
- **雲端儲存**時第三方無法存取
- **強式密碼自動產生**確保安全性

### 📦 高速壓縮節省儲存空間
- **Zstd 壓縮**實現高速與高壓縮率
- **Gzip 壓縮**注重相容性
- **無壓縮**選項也可選
- **磁碟空間最多減少 70%**（文字檔案的典型情況）

### ⚡ 增量備份實現超高速
- **僅備份變更的檔案**大幅縮短時間
- **SHA-256 雜湊**準確偵測變更
- **備份時間減少 90%**（第二次起，變更率 10% 的情況下）
- **儲存容量減少 85%**（僅儲存差異，典型情況）
- **自動切換到完整備份**（首次執行時）

### ⏰ 完全自動化的排程任務
- **設定後無需手動操作**自動執行
- **按重要性調整頻率**（每日、每週、每月）
- **完全防止忘記備份**
- **macOS launchd/Linux systemd 整合**實現可靠的自動執行

### 📊 清晰的管理與維護
- **備份了多少**透過統計檢視
- **何時執行**透過歷史記錄檢視
- **舊備份**自動刪除節省磁碟
- **資料損壞時**輕鬆復原

### 🌍 多語言支援
- **完全支援 4 種語言**：日語、英語、簡體中文（中國大陸）、繁體中文（台灣·香港）
- **自動語言偵測**：從 `LANG` 環境變數自動判斷（支援 `ja`、`en`、`zh-CN`、`zh-TW` 等）
- **所有訊息已翻譯**：CLI 輸出、錯誤訊息、說明文字均以各語言顯示

### 💡 易用的 CLI
- **拼字錯誤修正建議**：自動偵測指令名稱拼字錯誤並建議正確指令
  ```bash
  $ backup-suite restor
  error: unrecognized subcommand 'restor'

  Did you mean 'restore'?

  For more information, try '--help'.
  ```
- **智慧編輯距離演算法**：使用 Levenshtein 距離自動偵測相似指令（最多 2 個字元差異）
- **顏色支援**：根據終端顏色功能自動調整

## 螢幕截圖

### 說明介面
<img src="docs/screenshots/help.webp" alt="backup-suite help" width="600">

*以日語顯示指令列表和選項*

### 備份目標列表
<img src="docs/screenshots/list.webp" alt="backup-suite list" width="600">

*以表格形式顯示已註冊的備份目標*

### 備份執行
<img src="docs/screenshots/run.webp" alt="backup-suite run" width="600">

*實際備份執行介面*

### 備份執行（試執行）
<img src="docs/screenshots/dry-run.webp" alt="backup-suite dry-run" width="600">

*不實際複製檔案，僅確認執行內容*

### 備份歷史
<img src="docs/screenshots/history.webp" alt="backup-suite history" width="600">

*檢視過去的備份執行歷史*

## 安裝方法

### 透過 Homebrew 安裝（macOS）

```bash
brew tap sanae-abe/backup-suite
brew install backup-suite
```

### 透過 Cargo 安裝

```bash
# 啟用 Smart 功能安裝（推薦）
cargo install backup-suite --features smart

# 不啟用 Smart 功能安裝（輕量版）
cargo install backup-suite
```

### 從原始碼建置

```bash
# 1. 複製儲存庫
git clone git@github.com:sanae-abe/backup-suite.git
cd backup-suite

# 2. 安裝 Rust（如果尚未安裝）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source ~/.cargo/env

# 3. 建置並安裝（含 Smart 功能）
cargo build --release --features smart
cargo install --path . --features smart

# 4. 驗證執行
backup-suite --version
```

### 🌍 Shell 補完（多語言支援）

Shell 補完支援 **4 種語言**：英語、日語、簡體中文、繁體中文。

#### 快速設定（Zsh）

```bash
# 1. 建立補完目錄
mkdir -p ~/.zfunc

# 2. 新增至 ~/.zshrc
echo 'fpath=(~/.zfunc $fpath)' >> ~/.zshrc
echo 'autoload -Uz compinit && compinit' >> ~/.zshrc

# 3. 產生補完（從 $LANG 自動偵測語言）
backup-suite completion zsh > ~/.zfunc/_backup-suite

# 4. 重新啟動 shell
exec zsh
```

**手動選擇語言**：

```bash
# 日語
./scripts/generate-completion.sh ja

# 簡體中文
./scripts/generate-completion.sh zh-CN

# 繁體中文
./scripts/generate-completion.sh zh-TW

# 英語
./scripts/generate-completion.sh en
```

**故障排除**：

若補完無法運作，請參閱詳細指南 [docs/shell-completion.md](docs/shell-completion.md)。常見解決方案：

- **完全沒有補完**：重新啟動 shell（`exec zsh`），檢查檔案是否存在（`ls -la ~/.zfunc/_backup-suite`）
- **顯示錯誤的語言**：檢查 `echo $LANG`，或使用 `./scripts/generate-completion.sh zh-TW` 手動指定
- **compinit 警告**：修正權限（`chmod go-w ~/.zfunc`）

有關 Bash/Fish 安裝和詳細故障排除，請參閱 [docs/shell-completion.md](docs/shell-completion.md)。

## 快速入門

### 1. 基本設定
```bash
# 檢視目前設定
backup-suite status

# 設定檔位置
# ~/.config/backup-suite/config.toml
```

**注意**：語言由環境變數 `LANG` 自動偵測。支援的語言：日語、英語、簡體中文、繁體中文。在日語環境中會自動以日語顯示。

### 2. 設定備份儲存路徑

```bash
# 設定 Google Drive 儲存路徑
backup-suite config set-destination "/Users/你的使用者名稱/Library/CloudStorage/GoogleDrive-your@email.com/我的雲端硬碟/backup-storage"

# ⚠️ 重要：備份到雲端儲存時務必啟用加密
# 將備份儲存到 Google Drive 等雲端儲存時，
# 為防止第三方未授權存取，請務必使用 --encrypt 選項

# 檢視目前設定
backup-suite config get-destination
```

### 3. 驗證設定

若要驗證設定，請使用 [1. 基本設定](#1-基本設定) 中的 `backup-suite status` 指令。

## 基本使用

1. **新增檔案**
```bash
backup-suite add ~/Documents/project --priority high --category development
backup-suite add ~/Photos --priority medium --category personal
```

2. **檢視目標列表**
```bash
backup-suite list
backup-suite list --priority high  # 僅高優先順序
```

3. **更新目標設定**
```bash
# 僅變更類別
backup-suite update ~/.config --category "設定檔案"

# 同時變更優先順序和類別
backup-suite update ~/.ssh --priority high --category "SSH設定（含私密金鑰）"

# 新增排除模式
backup-suite update ~/.ssh --exclude "known_hosts*" --exclude "*.old"

# 同時更新多個設定
backup-suite update ~/Documents --priority high --category "重要文件" --exclude "*.tmp"
```

4. **執行備份**
```bash
backup-suite run                   # 執行所有目標
backup-suite run --priority high   # 僅高優先順序
backup-suite run --category work   # 僅特定類別
backup-suite run --dry-run         # 試執行（僅確認）

# 增量備份
backup-suite run --incremental     # 僅備份變更部分（第二次起推薦）

# 壓縮選項
backup-suite run --compress zstd   # Zstd 壓縮（高速·高壓縮率·推薦）
backup-suite run --compress gzip   # Gzip 壓縮（注重相容性）
backup-suite run --compress none   # 無壓縮

# 加密備份（推薦：互動式密碼提示）
backup-suite run --encrypt
# → 透過提示安全輸入密碼（不會儲存在 Shell 歷史記錄中）

# 或使用環境變數（選擇性）
export BACKUP_SUITE_PASSWORD="your-secure-password"
backup-suite run --encrypt

# 壓縮 + 加密組合
backup-suite run --compress zstd --encrypt
# → 透過提示互動式輸入密碼
```

5. **自動化設定**
```bash
# 按優先順序設定排程
backup-suite schedule setup --high daily --medium weekly --low monthly
backup-suite schedule enable
```

## 🤖 Smart 功能（智慧備份）

透過統計異常偵測和檔案重要性分析最佳化備份。

### 安裝

要使用 Smart 功能，需要在建置時使用 `--features smart` 旗標。

```bash
# 啟用 Smart 功能建置
cargo build --release --features smart
cargo install --path . --features smart

# 或透過 Cargo 安裝
cargo install backup-suite --features smart
```

### 主要功能

#### 1. Smart 自動設定

分析目錄並自動產生最佳備份設定。

```bash
# 自動分析和設定（分別評估子目錄）
backup-suite smart auto-configure ~/data

# 以互動方式確認並設定（確認子目錄和排除模式）
backup-suite smart auto-configure ~/data --interactive

# 試執行（不套用設定，僅確認）
backup-suite smart auto-configure ~/data --dry-run

# 指定子目錄探索深度（預設：1）
backup-suite smart auto-configure ~/data --max-depth 2

# 指定要處理的子目錄最大數（預設：100）
backup-suite smart auto-configure ~/data --max-subdirs 50

# 大量子目錄時增加處理數上限
backup-suite smart auto-configure ~/data --max-subdirs 200
```

**功能**：
- **分別評估每個子目錄的重要性**（為每個目錄設定最佳優先順序）
- **自動偵測並套用排除模式**（自動排除 `node_modules/`、`target/`、`.cache/` 等）
- **自動判斷專案類型**（Rust、Node.js、Python 等）
- **僅套用信賴度 80% 以上的模式**（防止誤檢）
- **透過處理數限制進行效能最佳化**（`--max-subdirs` 應對大量子目錄，預設：100）

**輸出範例**：
```
🤖 Smart 自動設定
分析中："/Users/user/projects"
  📁 發現 3 個子目錄：3
    評估中："/Users/user/projects/web-app"
      建議優先順序：High（分數：95）
      📋 排除模式建議：3
        - node_modules（99.0%，預計減少 2.34 GB）
        - .cache（95.0%，預計減少 0.45 GB）
        - .*\.tmp$（99.0%，預計減少 0.00 GB）
      📝 排除模式：node_modules、.cache、.*\.tmp$
      ✅ 已加入設定
    評估中："/Users/user/projects/rust-cli"
      建議優先順序：High（分數：95）
      📋 排除模式建議：2
        - target（99.0%，預計減少 1.87 GB）
        - .cache（95.0%，預計減少 0.12 GB）
      📝 排除模式：target、.cache
      ✅ 已加入設定
    評估中："/Users/user/projects/archive"
      建議優先順序：Low（分數：30）
      ✅ 已加入設定

自動設定已完成
  加入的項目：3
  總減少量：4.78 GB（備份時間約縮短 35%）
```

**子目錄數達到限制時的範例**：
```
🤖 Smart 自動設定
分析中："/Users/user/large-project"
  📁 發現 100 個子目錄：100
  ⚠️  已達到限制，部分子目錄未處理：100（可使用 --max-subdirs 變更）
```

#### 2. 檔案重要性分析

按重要程度對目錄中的檔案進行分類，最佳化備份策略。

```bash
# 分析目錄重要性
backup-suite smart analyze ~/documents

# 顯示詳細的重要性分數
backup-suite smart analyze ~/documents --detailed

# 僅分析特定檔案類型
backup-suite smart analyze ~/projects --filter "*.rs,*.toml"
```

**評估標準**：
- **高重要性（80-100 分）**：原始碼、文件、設定檔
- **中重要性（40-79 分）**：影像、資料檔案
- **低重要性（0-39 分）**：日誌、暫存檔案

**輸出範例**：
```
🤖 Smart 檔案重要性分析：~/Documents

┌─────────────────────────┬──────────────┬──────────┬─────────────────────┐
│ 檔案/目錄                │ 重要性分數    │ 建議優先順序 │ 理由                 │
├─────────────────────────┼──────────────┼──────────┼─────────────────────┤
│ src/                    │ ████████ 95  │ 高        │ 原始碼（頻繁更新）    │
│ reports/                │ ████████ 90  │ 高        │ 文件（重要）          │
│ photos/                 │ ████░░░░ 60  │ 中        │ 影像檔案              │
│ .cache/                 │ █░░░░░░░ 10  │ 建議排除  │ 快取目錄              │
└─────────────────────────┴──────────────┴──────────┴─────────────────────┘
```

**效能**：約 8 秒（10,000 個檔案）

#### 3. 排除模式建議

自動偵測不必要的檔案，建議排除模式。

```bash
# 顯示排除模式建議
backup-suite smart suggest-exclude ~/projects

# 自動將建議模式套用到設定檔
backup-suite smart suggest-exclude ~/projects --apply

# 指定最小檔案大小（預設：100MB）
backup-suite smart suggest-exclude ~/projects --min-size 50MB
```

**偵測目標**：
- 建置產物（`target/`、`dist/`、`build/`）
- 相依快取（`node_modules/`、`.cargo/`）
- 暫存檔案（`*.tmp`、`*.cache`）
- 大容量媒體檔案（超過閾值大小）

**輸出範例**：
```
🤖 Smart 排除模式建議：~/projects

┌──────────────────┬──────────┬──────────┬─────────────────────┐
│ 模式              │ 減少量    │ 信賴度    │ 理由                 │
├──────────────────┼──────────┼──────────┼─────────────────────┤
│ node_modules/    │ 2.34 GB  │ 99%      │ npm 相依（可重新產生）│
│ target/          │ 1.87 GB  │ 99%      │ Rust 建置產物        │
│ .cache/          │ 0.45 GB  │ 95%      │ 快取目錄              │
└──────────────────┴──────────┴──────────┴─────────────────────┘

💡 總減少量：4.66 GB（備份時間約縮短 30%）
```

#### 4. 異常偵測

從歷史記錄中偵測統計上異常的備份。

```bash
# 偵測過去 7 天的異常
backup-suite smart detect --days 7

# 更詳細的分析（也顯示統計資訊）
backup-suite smart detect --days 14 --detailed
```

**偵測內容**：
- 備份大小的急增/急減（Z-score 統計分析）
- 磁碟容量耗盡預測（線性迴歸）
- 失敗模式分析（按類別和時間）

**輸出範例**：
```
🤖 Smart 異常偵測報告（過去 7 天）

┌────┬──────────────────┬──────────┬──────────┬─────────────────────┐
│ No │ 偵測時間          │ 異常類型  │ 信賴度    │ 說明                 │
├────┼──────────────────┼──────────┼──────────┼─────────────────────┤
│ 1  │ 2025-11-09 03:15 │ 大小激增  │ 95.3%    │ 檔案大小為正常的3倍   │
└────┴──────────────────┴──────────┴──────────┴─────────────────────┘

📊 摘要：偵測到 1 個異常
💡 建議操作：將 ~/Downloads 的暫存檔案加入排除設定
```

**效能**：< 1ms（100 條歷史記錄）

### 停用 Smart 功能

如果不需要 Smart 功能，請使用標準建置。

```bash
# 標準建置（無 Smart 功能）
cargo build --release
cargo install --path .
```

### 安全與隱私

所有 Smart 功能**完全離線**執行：

- ✅ 外部 API 呼叫：無
- ✅ 雲端服務：不需要
- ✅ 敏感資訊傳輸：零
- ✅ 資料收集：無

詳情請參閱 [Smart 功能文件](docs/smart/features.md)。

## 設定檔

### ~/.config/backup-suite/config.toml 範例
```toml
[general]
log_level = "info"
log_file = "~/.local/share/backup-suite/logs/backup.log"

[storage]
type = "local"
path = "/Users/john/Library/CloudStorage/GoogleDrive-john@example.com/我的雲端硬碟/backup-storage"  # 使用雲端儲存時 encryption = true 是必需的
compression = "zstd"  # 壓縮類型："zstd"、"gzip"、"none"
compression_level = 3  # 壓縮等級：1-22（Zstd）、1-9（Gzip）
encryption = true
encryption_key_file = "~/.config/backup-suite/keys/backup.key"  # 重要：使用 chmod 600 保護

[schedule]
enabled = true
daily_time = "02:00"
weekly_day = "sunday"
monthly_day = 1

[targets]
[[targets.directories]]
name = "documents"
path = "~/Documents"
exclude = ["*.tmp", "*.cache", ".DS_Store"]

[[targets.directories]]
name = "projects"
path = "~/Projects"
exclude = ["node_modules/", "target/", ".git/", "*.log"]
```

## 排程任務功能

### 自動備份設定

```bash
# 設定排程頻率
backup-suite schedule setup --high daily --medium weekly --low monthly

# 啟用排程
backup-suite schedule enable

# 檢視狀態
backup-suite schedule status
```

### 各平台的運作方式

#### macOS (launchd)
- 設定檔：`~/Library/LaunchAgents/com.backup-suite.{priority}.plist`
- 日誌：`/tmp/backup-suite-{priority}.log`
- 確認：`launchctl list | grep backup-suite`

#### Linux (systemd)
- 設定檔：`~/.config/systemd/user/backup-suite-{priority}.{service,timer}`
- 日誌：`journalctl --user -u backup-suite-{priority}.service`
- 確認：`systemctl --user list-timers backup-suite-*`

詳情請參閱[排程任務指南](docs/SCHEDULER.md)。

## 指令參考

| 指令           | 說明                       | 範例                                            |
| -------------- | -------------------------- | ----------------------------------------------- |
| **add**        | 新增備份目標               | `backup-suite add ~/docs --priority high`       |
| **list, ls**   | 顯示目標列表               | `backup-suite list --priority medium`           |
| **remove**     | 刪除目標                   | `backup-suite remove ~/old-files`               |
| **update**     | 更新目標設定               | `backup-suite update ~/.ssh --priority high --category "SSH設定"` |
| **clear, rm**  | 批次刪除                   | `backup-suite clear --priority low`             |
| **run**        | 執行備份                   | `backup-suite run --encrypt`                    |
| **restore**    | 復原備份                   | `backup-suite restore --from backup-20251104`   |
| **cleanup**    | 刪除舊備份                 | `backup-suite cleanup --days 30`                |
| **status**     | 顯示目前狀態               | `backup-suite status`                           |
| **history**    | 顯示執行歷史               | `backup-suite history --days 7`                 |
| **schedule**   | 排程任務管理               | `backup-suite schedule enable`                  |
| **config**     | 設定管理                   | `backup-suite config set-destination ~/backups` |
| **open**       | 開啟備份目錄               | `backup-suite open`                             |
| **completion** | 產生 shell 補完            | `backup-suite completion zsh`                   |
| **smart**         | Smart 功能（需要 `--features smart`）| `backup-suite smart detect --days 7`            |

## 更新與移除

### 更新

```bash
# Homebrew
brew upgrade backup-suite

# Cargo
cargo install backup-suite --force --features smart

# 從原始碼
cd backup-suite
git pull origin main
cargo install --path . --force --features smart
```

### 移除

```bash
# 1. 刪除二進位檔案
rm ~/.local/bin/backup-suite

# 2. 刪除設定檔（選擇性）
rm -rf ~/.config/backup-suite/

# 3. 刪除日誌檔案（選擇性）
rm -rf ~/.local/share/backup-suite/
```

## 安全與品質

### **企業級安全**
- **加密**：AES-256-GCM（認證加密）
- **金鑰衍生**：Argon2id（記憶體成本 19MB，反覆運算 2 次）
- **Nonce 衝突偵測**：偵錯建置自動追蹤（發行建置零額外負擔）
  - 追蹤所有 Nonce（加密初始化向量）並立即偵測衝突
  - 發生衝突時提供詳細錯誤訊息說明安全影響
  - 發行建置時在編譯時完全刪除（無效能影響）
- **路徑遍歷防護**：Unicode 正規化（NFKC）、Null byte 偵測、符號連結攻擊防護
- **敏感資料清除**：使用 Zeroize（記憶體傾印攻擊防護）
- **僅本機**：不依賴雲端，安全可靠
- **權限管理**：設定檔的適當權限設定

### **型別安全與記憶體安全**
- Rust 強大的型別系統最大程度減少執行階段錯誤
- 記憶體安全保證（防止緩衝區溢位、記憶體洩漏）
- 編譯時期錯誤偵測
- 敏感資料的安全清除（ZeroizeOnDrop）

### **防禦目標攻擊**
- ✅ **路徑遍歷攻擊**：Unicode 正規化（NFKC）、Null byte 偵測、符號連結驗證
- ✅ **暴力破解攻擊**：Argon2id 高成本金鑰衍生（記憶體 19MB，反覆運算 2 次）
- ✅ **記憶體傾印攻擊**：透過 Zeroize 立即清除敏感資料
- ✅ **中間人攻擊**：AES-256-GCM 認證加密（偵測竄改）
- ✅ **時序攻擊**：透過常數時間比較驗證金鑰
- ✅ **注入攻擊**：透過 Rust 型別系統實現安全的路徑操作

## 技術堆疊

- **語言**：Rust（最新穩定版）
- **CLI**：clap 4.x（指令列解析·補完產生）
- **壓縮**：Zstd（高速·高壓縮率）、Gzip（相容性）
- **加密**：AES-256-GCM、Argon2
- **設定**：TOML（人類可讀的設定格式）
- **排程任務**：macOS launchctl、Linux systemd
- **Smart/統計分析**：statrs（統計計算）、rayon（並行處理）

## 支援平台

| 作業系統  | 架構           | 支援狀態   |
| --------- | -------------- | ---------- |
| 🐧 Linux  | x86_64         | ✅ 完全支援 |
| 🐧 Linux  | aarch64        | ✅ 完全支援 |
| 🍎 macOS  | x86_64         | ✅ 完全支援 |
| 🍎 macOS  | Apple Silicon  | ✅ 完全支援 |

## 開源授權

本專案採用 [MIT 授權](LICENSE) 開源。

---

## 貢獻

歡迎提交 Bug 報告、功能請求和 Pull Request。
請透過 GitHub Issues 或 PR 隨時聯絡我們。
